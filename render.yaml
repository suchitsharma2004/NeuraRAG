services:
  - type: web
    name: neurarag
    env: python
    buildCommand: "./build.sh"
    startCommand: "cd RAG && gunicorn RAG.wsgi:application"
    envVars:
      - key: DEBUG
        value: False
      - key: DJANGO_SETTINGS_MODULE
        value: RAG.settings

# PostgreSQL database removed for free deployment
# Uncomment below if you want to add paid PostgreSQL later:
#
#  - type: pserv
#    name: neurarag-db
#    env: postgresql
#    plan: starter
